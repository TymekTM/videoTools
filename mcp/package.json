{
  "name": "video-tools-mcp",
  "version": "0.1.0",
  "description": "MCP server for AI agents to generate videos - newspaper, chat, typing, chart, map, notification animations",
  "main": "server.js",
  "bin": {
    "video-tools-mcp": "./server.js"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node test.js",
    "postinstall": "npx puppeteer browsers install chrome@stable"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "puppeteer": "^24.8.2",
    "ffmpeg-static": "^5.3.0"
  }
}
